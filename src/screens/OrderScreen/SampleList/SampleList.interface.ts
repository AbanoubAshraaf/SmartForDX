// I written this file using types to enable you know that I can write more than one code style

import { ISample } from '../../../models/order';

export type SampleListProps = {
    readonly samples: readonly ISample[];
};
